# Git Large File Storage — track the trained model binary
# This ensures cnn_model.h5 (40MB) is stored efficiently in git
ml_models/*.h5 filter=lfs diff=lfs merge=lfs -text
ml_models/*.keras filter=lfs diff=lfs merge=lfs -text
